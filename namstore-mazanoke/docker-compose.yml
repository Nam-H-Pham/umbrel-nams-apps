services:
  app_proxy:
    environment:
      APP_HOST: namstore-mazanoke_web_1
      APP_PORT: 80
    depends_on:
      - web

  web:
    image: ghcr.io/civilblur/mazanoke:latest
    restart: unless-stopped
