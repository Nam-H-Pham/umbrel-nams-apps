# Neolink for Umbrel

This app runs Neolink as a local RTSP bridge.

## Configure

After installing the app, put your local camera config at:

```bash
~/umbrel/app-data/namstore-neolink/data/neolink.toml
```

Then restart the app.

## Stream URL

```text
rtsp://YOUR_UMBREL_IP:8554/e321
```
